# ALEMBIC_REPORT

## Resultats par etape

| Etape | Resultat | Detail |
| --- | --- | --- |
| 1 - Installation | ✅ | `pip install alembic==1.13.3` OK. `alembic --version` retourne `alembic 1.13.3`. |
| 2 - Initialisation | ✅ | `alembic init alembic` a cree `alembic/`, `alembic/versions/` et `alembic.ini`. |
| 3 - Configuration `alembic.ini` | ✅ | `sqlalchemy.url = postgresql+psycopg2://arbo:arbo@localhost:5432/arbo`. |
| 4 - Configuration `alembic/env.py` | ✅ | `Base.metadata` est branche, `ARBO_DATABASE_URL` peut surcharger l'URL, PostGIS/Tiger extra tables sont ignorees par autogenerate. |
| 5 - Migration initiale | ✅ | Migration creee : `alembic/versions/4553206819f7_initial_schema.py`. Elle versionne les 7 tables ARBO. |
| 6 - Stamp base existante | ✅ | `alembic stamp head` OK. Verification initiale : `4553206819f7 (head)`. |
| 7 - Mise a jour `main.py` | ✅ | `init_db()` n'est plus appele au demarrage FastAPI; les migrations sont gerees par Alembic. |
| 8 - Test final | ✅ | Uvicorn demarre avec `Database migrations are managed by Alembic.` et sans `Database schema verified`. Migration `test_pipeline` vide. |
| 9 - Rapport | ✅ | Rapport genere dans `D:\ARBO\ALEMBIC_REPORT.md`. |

## Output `alembic current`

Apres generation et application de la migration vide `test_pipeline` :

```text
4fb4c643ead5 (head)
```

Note : juste apres le stamp initial, avant la migration de test, `alembic current` affichait :

```text
4553206819f7 (head)
```

## Migration initiale `upgrade()`

Fichier : `alembic/versions/4553206819f7_initial_schema.py`

Tables creees par `upgrade()` pour une base neuve :

- `users`
- `farms`
- `plots`
- `tree_lines`
- `carbon_projects`
- `carbon_credits`
- `transactions`

Autres objets versionnes :

- Extension `postgis` via `CREATE EXTENSION IF NOT EXISTS postgis`
- Enums PostgreSQL : `user_role`, `kyc_status`, `project_status`, `credit_status`, `transaction_status`
- Index applicatifs SQLAlchemy : emails, FK indexes, serial numbers, credit transaction unique index

## Migration `test_pipeline` `upgrade()`

Fichier : `alembic/versions/4fb4c643ead5_test_pipeline.py`

```python
def upgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    pass
    # ### end Alembic commands ###
```

`downgrade()` est egalement vide avec `pass`.

## Derives detectees

- Premier autogenerate initial : Alembic a detecte des tables PostGIS/Tiger comme `topology`, `addr`, `county`, `place`, `edges`, etc. comme "removed tables".
- Action prise : aucune suppression appliquee. `env.py` exclut maintenant les tables refletees absentes des modeles ARBO afin d'eviter de generer des `drop_table()` sur les objets geres par les extensions PostGIS/Tiger.
- Migration `test_pipeline` : aucune derive de schema applicatif detectee; `upgrade()` est vide.

## Confirmation Uvicorn

Redemarrage avec :

```text
python -m uvicorn backend.main:app --reload
```

Logs verifies :

```text
Starting ARBO platform...
Database migrations are managed by Alembic.
Run: alembic upgrade head
Application startup complete.
```

Le demarrage ne contient plus `Database schema verified`; `create_all()` n'est plus execute par FastAPI.
